### `in` operator for substring check
Check if a string contains a substring
- `substring in string` returns `True` or `False`
- Example: `"abc" in "xabcy"` → `True`
- No need for `find()` or slicing for simple containment checks
- Also works with lists: `3 in [1, 2, 3]` → `True`
